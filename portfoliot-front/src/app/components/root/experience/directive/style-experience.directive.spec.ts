import { StyleExperienceDirective } from './style-experience.directive';

describe('StyleExperienceDirective', () => {
  it('should create an instance', () => {
    const directive = new StyleExperienceDirective();
    expect(directive).toBeTruthy();
  });
});
