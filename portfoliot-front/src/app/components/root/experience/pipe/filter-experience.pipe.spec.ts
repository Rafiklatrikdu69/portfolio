import { FilterExperiencePipe } from './filter-experience.pipe';

describe('FilterExperiencePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterExperiencePipe();
    expect(pipe).toBeTruthy();
  });
});
